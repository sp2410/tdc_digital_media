<%= bootstrap_form_for @blog  do |form| %>   
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  

    <div class="field">    
      <%= form.text_field :title, id: :blog_title, :placeholder => "Input a Title" %>
    </div>
  
  
    <div class="field">    
      <%= form.cktext_area :body, :as => :ckeditor, input_html: {:ckeditor => {:toolbar => 'FULL'}}, class: "form-control", :placeholder => "Input Body Here" %>
    </div>



  <div class="actions">
    <%= form.submit :class => "btn btn-default btn-lg", :style => "margin: 2em;"  %>
  </div>
<% end %>
